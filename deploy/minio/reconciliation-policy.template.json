{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "AllowPrefixList",
      "Effect": "Allow",
      "Action": [
        "s3:ListBucket"
      ],
      "Resource": [
        "arn:aws:s3:::${BUCKET}"
      ],
      "Condition": {
        "StringLike": {
          "s3:prefix": [
            "${PREFIX}",
            "${PREFIX}/*"
          ]
        }
      }
    },
    {
      "Sid": "AllowPrefixObjects",
      "Effect": "Allow",
      "Action": [
        "s3:GetObject",
        "s3:PutObject",
        "s3:DeleteObject"
      ],
      "Resource": [
        "arn:aws:s3:::${BUCKET}/${PREFIX}/*"
      ]
    }
  ]
}
